version: "3.8"

services:
  crop-disease-app:
    build: .
    container_name: crop_disease_detector
    ports:
      - "5000:5000"
    volumes:
      - ./model:/app/model
      - ./static/uploads:/app/static/uploads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped